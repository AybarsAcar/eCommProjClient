import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    maxWidth: 450,
    marginRight: "auto",
    marginLeft: "auto",
    marginTop: "4rem",
  },
  media: {
    height: 250,
  },
});